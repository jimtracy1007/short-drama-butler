# Storyboard 交接包要求

每个 `storyboard-package.md` 必须包含：

1. 固定参数：3—8 岁、16:9、120 秒、少儿温暖安全、无字幕/Logo/水印。
2. 镜头约束：镜头数和时长服务于剧情、动作、对白和情绪，避免无意义碎镜头。
3. 本集剧情需求、不可改动设定和本集状态。
4. 素材表：ID、名称、类别、范围、图片路径；只允许表中资产作为既有设定。
5. 明确的下一步：调用 `$seedance-storyboard-generator`，先审大纲，再写剧本，最后拆镜头表和关键帧提示词。

